server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mausamRayamajhi/microservices-Hystrix-Zipkin-API-Gateway
          clone-on-start: true