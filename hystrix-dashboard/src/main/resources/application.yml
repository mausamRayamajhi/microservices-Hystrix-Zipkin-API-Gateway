spring:
  application:
    name: hystrix-dashboard
  config:
    enable: true
    import: optional:configserver:http://localhost:9296/
server:
  port: 9295

hystrix:
  dashboard:
    proxy-stream-allow-list: "*"